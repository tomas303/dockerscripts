version: '3'

includes:
  def:
    taskfile: ./Taskfile.def.yml
    optional: false
  base:
    taskfile: ../tasks/Taskfile.base.yml
    dir: ..
    optional: false

vars:
  CNT_BASE: "asvim"
  CNT_SUFFIX: "test"
  CNT_APP: "asvim"

tasks:
  rebuild:
    desc: Rebuild new astronvim distrobox container
    cmds:
      - task: base:full-rebuild
